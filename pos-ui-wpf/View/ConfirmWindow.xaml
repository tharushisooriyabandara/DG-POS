<Window x:Class="POS_UI.View.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" Width="480" WindowStartupLocation="CenterOwner" SizeToContent="Height"
        ResizeMode="NoResize" WindowStyle="None" ShowInTaskbar="False" Background="White">
    <Border Background="White" CornerRadius="20" Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with title and close -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" Width="28" Height="28" Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Right" VerticalAlignment="Top" Click="OnClose"/>
            </Grid>

            <!-- Message -->
            <TextBlock Grid.Row="1" Text="{Binding Message}" TextWrapping="Wrap" FontSize="16" Margin="0,6,0,6"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="No" Margin="0,0,8,0" Width="110" Height="36" Click="OnNo"/>
                <Button Content="Yes" Background="#1976D2" Foreground="White" Width="110" Height="36" Click="OnYes"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>


