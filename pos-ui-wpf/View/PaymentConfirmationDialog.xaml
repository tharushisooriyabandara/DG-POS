<UserControl x:Class="POS_UI.View.PaymentConfirmationDialog"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <Grid Width="350" Height="250" Margin="20" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,0">
            <TextBlock Text="Payment Confirmation" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Did you receive the money?" 
                       FontSize="16" 
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Foreground="#666666"/>
        </StackPanel>
        
        <!-- Order Information -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,10,0,10">
            <TextBlock Text="{Binding Order.CustomerName, StringFormat='Customer: {0}'}" 
                       FontSize="14"
                       Margin="0,5"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Order.OrderNumber, StringFormat='Order: {0}'}" 
                        FontSize="14"
                        Margin="0,5"
                        HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Order.DisplayTotal, StringFormat='Amount: ${0:F2}'}" 
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,5"
                       HorizontalAlignment="Center"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Yes" 
                    Command="{Binding YesCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#1976D2"
                    Foreground="White"
                    Width="100" 
                    Height="36" 
                    Margin="0,0,10,0"/>
            <Button Content="No" 
                    Command="{Binding NoCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#1976D2"
                    Foreground="White"
                    Width="100" 
                    Height="36" 
                    Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
