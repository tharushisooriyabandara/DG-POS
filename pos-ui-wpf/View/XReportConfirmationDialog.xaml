<UserControl x:Class="POS_UI.View.XReportConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             MinWidth="400"
             MaxWidth="500">

    <Border Background="White" CornerRadius="5" Padding="24" BorderBrush="#CCCCCC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Message -->
            <TextBlock Grid.Row="0"
                       Text="This report is generated based on completed orders only. Do you want to generate it?"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       FontSize="16"
                       Margin="0,0,0,24"
                       Foreground="#333"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,0">
                <Button Content="Cancel" 
                        Width="120" 
                        Height="40"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#EEEEEE"
                        Foreground="#333"
                        Click="CancelButton_Click"
                        Margin="0,0,12,0"/>
                <Button Content="Generate" 
                        Width="120" 
                        Height="40"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#1976D2"
                        Foreground="White"
                        Click="GenerateButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

