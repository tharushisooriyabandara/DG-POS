<UserControl x:Class="POS_UI.View.NoteDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel>
            <TextBlock Text="Set Note" 
                       FontSize="25" 
                       FontWeight="Bold" 
                       Margin="0,0,0,16"/>
            <Button DockPanel.Dock="Right" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource MaterialDesignToolButton}" Background="Transparent" BorderBrush="Transparent" Width="37" Margin="0,-12,-12,0">
                <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
            </Button>
        </DockPanel>

        <TextBlock Grid.Row="1"
            Text="Note : " FontSize="15" FontWeight="SemiBold" Margin="0,0,0,16"/>
        <!-- Note TextBox -->
        <StackPanel Grid.Row="2" Margin="8,0,8,0">

            <Border 
            BorderBrush="{DynamicResource MaterialDesignOutlineBorderBrush}" 
            BorderThickness="1" 
            CornerRadius="5"
            Padding="10"
            Background="{DynamicResource MaterialDesignPaper}"
                         Width="367" Height="60">
                    <DockPanel LastChildFill="True">
                        <materialDesign:PackIcon 
                            Kind="Note" 
                            Width="20" 
                            Height="20" 
                            VerticalAlignment="Top" 
                            Margin="0,2,10,0"/>
            
                        <TextBox 
                            Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}"
                            MaxLength="100"
                            materialDesign:HintAssist.Hint="Enter Note Here"
                            TextWrapping="Wrap"
                            AcceptsReturn="True"
                            VerticalScrollBarVisibility="Auto"
                            BorderThickness="0"
                            Background="Transparent"
                            Padding="0"
                            FontSize="14"/>
                    </DockPanel>
                </Border>
        </StackPanel>


        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,0">
            <Button Content="Cancel"
                    Width="100" Height="36" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding CancelCommand}" Margin="0,0,10,0"/>
            <Button Content="Save"
                    Width="100" Height="36" Background="#1976D2" Foreground="White" FontWeight="Bold" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</UserControl> 